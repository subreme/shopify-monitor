// This module will host all the tests for the project.

// The `mod.rs` file is used to mark a directory as a module, allowing
// the tests to be placed in individual files, which will be convenient
// once more tests are added.

mod available_product;
mod color;
